import numpy as np

# transpose() function with 1-D array:
a = np.array([1, 2, 3])
b = np.transpose(a)
print(b)
print()
#transpose() function to transpose a 2-D array (or a matrix):
a = np.array([
    [1, 2, 3],
    [4, 5, 6]
])

b = np.transpose(a)
print(b)